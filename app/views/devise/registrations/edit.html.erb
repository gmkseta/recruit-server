<div class="card p-3 col-md-8 col-sm-12 mx-auto mt-3 pb-5">
  <div class="auth-form mx-5 no-float">
    <div class="row">
      <div class="col-sm-12 col-xs-12">
        <div class="mb-30">
  				<h3 class="text-center txt-dark mb-10">회원 정보 수정</h3>
  				<h6 class="text-center nonecase-font txt-grey">회원 정보를 입력해주세요</h6>
        </div>
        <hr>
        
        <div class="form-wrap mt-5">
          <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
            <%= render "devise/shared/error_messages", resource: resource %>

            <div class="form-group">
              
              <%= f.label :email %><br />
              <%= f.email_field :email, class: "form-control", autofocus: true, readonly: true %>
            </div>
            
            <div class="form-group">
              <%= f.label :name, "이름" %><br />
              <%= f.text_field :name, class: "form-control" %>
            </div>
            
            <div class="form-group">
              <%= f.label :phone, "전화번호" %><br />
              <%= f.text_field :phone, class: "form-control" %>
            </div>
            
            <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
              <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
            <% end %>

            <div class="form-group">
              <%= f.label :password %> <i>(기존 비밀번호를 유지할 경우 빈 칸으로 남겨주세요)</i><br />
              <%= f.password_field :password, class: "form-control", autocomplete: "new-password" %>
              <% if @minimum_password_length %>
                <em><%= @minimum_password_length %>글자 이상 입력하세요</em>
              <% end %>
            </div>

            <div class="form-group">
              <%= f.label :password_confirmation %><br />
              <%= f.password_field :password_confirmation, class: "form-control", autocomplete: "new-password" %>
            </div>

            <div class="form-group">
              <%= f.label :current_password %> <i>(정보를 수정하시려면 현재 비밀번호를 입력해주세요)</i><br />
              <%= f.password_field :current_password, class: "form-control", autocomplete: "current-password" %>
            </div>

            <div class="actions">
              <%= f.submit "정보 수정", class: "btn btn-lg btn-primary btn-block" %>
            </div>
          <% end %>
          
        </div>
        
      </div>
    </div>
  </div>
</div>
<div class="card p-3 col-md-8 col-sm-12 mx-auto mt-5 text-center">
  <h3>계정 탈퇴</h3>
  <%= button_to "탈퇴하기", registration_path(resource_name), class: "btn btn-outline-danger", data: { confirm: "정말 탈퇴하시겠습니까? 탈퇴할 경우 팀 데이터도 같이 삭제됩니다." }, method: :delete %>
</div>

